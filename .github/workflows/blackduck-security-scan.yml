name: Black Duck Security Scan

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:
    inputs:
      fail_on_severity:
        description: 'Fail build on severity (BLOCKER|CRITICAL|MAJOR|MINOR|NONE)'
        required: false
        default: 'CRITICAL'
        type: choice
        options:
          - NONE
          - MINOR
          - MAJOR
          - CRITICAL
          - BLOCKER

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  blackducksca:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Cache Black Duck data
        uses: actions/cache@v4
        with:
          path: |
            ~/.blackduck
            **/blackduck
          key: blackduck-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            blackduck-${{ runner.os }}-

      - name: Black Duck security scan
        id: black-duck-security-scan
        uses: blackduck-inc/black-duck-security-scan@v2
        with:
          blackducksca_url: ${{ vars.BLACKDUCKSCA_URL }}
          blackducksca_token: ${{ secrets.BLACKDUCKSCA_TOKEN }}
          blackducksca_scan_full: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
          blackducksca_scan_failure_severities: ${{ github.event.inputs.fail_on_severity || 'CRITICAL' }}
          mark_build_status: ${{ github.event_name == 'pull_request' }}
        env:
          DETECT_PROJECT_VERSION_NAME: ${{ github.ref_name }}
          DETECT_CODE_LOCATION_NAME: ${{ github.repository }}-${{ github.ref_name }}
        continue-on-error: ${{ github.event_name == 'schedule' }}

      - name: Upload scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: blackduck-scan-results-${{ github.run_number }}
          path: |
            **/blackduck/
            **/Black_Duck_Notices_Report.txt
          retention-days: 30

      - name: Security scan summary
        if: always()
        run: |
          echo "## 🔒 Black Duck Security Scan Results" > "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Branch**: ${{ github.ref_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Scan Type**: ${{ github.event_name == 'schedule' && 'Scheduled Full Scan' || (github.event_name == 'workflow_dispatch' && 'Manual Full Scan' || 'On-Change Scan') }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Status**: ${{ steps.black-duck-security-scan.outcome }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Review detailed results in Black Duck or the uploaded workflow artifacts." >> "$GITHUB_STEP_SUMMARY"
