import React, { createContext, useContext, ReactNode } from 'react'; import { useFamilyStats } from '../hooks/useFamilyStats'; const FamilyStatsContext = createContext(null); export function FamilyStatsProvider({ children }) { const stats = useFamilyStats(); return <FamilyStatsContext.Provider value={stats}>{children}</FamilyStatsContext.Provider>; } export function useFamilyStatsContext() { const context = useContext(FamilyStatsContext); if (!context) { throw new Error('useFamilyStatsContext must be used within a FamilyStatsProvider'); } return context; }
